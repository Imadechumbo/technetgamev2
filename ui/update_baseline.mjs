import fs from 'fs';import path from 'path';import { fileURLToPath } from 'url';const __filename=fileURLToPath(import.meta.url);const __dirname=path.dirname(__filename);const root=path.resolve(__dirname,'..');const currentDir=path.join(root,'ui','current');const baseDir=path.join(root,'ui','baselines');if(!fs.existsSync(currentDir))throw new Error(`Diretório de capturas não encontrado: ${currentDir}`);fs.mkdirSync(baseDir,{recursive:true});for(const file of fs.readdirSync(currentDir))if(file.endsWith('.png'))fs.copyFileSync(path.join(currentDir,file),path.join(baseDir,file));console.log('Baseline atualizado em ui/baselines');